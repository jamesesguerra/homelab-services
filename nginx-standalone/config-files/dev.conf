server {
    listen                  80;
    listen             [::]:80;
    server_name             booked-up-dev.homelab;

    location / {
        root   /usr/share/nginx/html/dev;
        index  index.html index.html;
    }
}